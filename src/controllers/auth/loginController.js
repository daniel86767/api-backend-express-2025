export const loginController = async (req,res) => {

    //receber o email e a senha batem com o que está no banco de dados

    // se bater gerar um token(Jwt)

    // enviar o token para 
    return
}